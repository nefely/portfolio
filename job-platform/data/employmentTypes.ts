import type { EmploymentType } from "@/types/job";

// Лейбли — в messages/*.json (namespace "employmentType").
export const EMPLOYMENT_TYPE_IDS: EmploymentType[] = [
  "full-time",
  "part-time",
  "project",
  "seasonal",
];
